return {
  'zbirenbaum/copilot.lua',
  cmd = 'Copilot',
  event = 'VeryLazy',
  opts = {
    -- Options recommended for copilot-cmp
    suggestion = {
      enabled = false,
    },
    panel = {
      enabled = false,
    },
  },
}
